{
  plugins.lualine = {
    enable = true;
    settings = {
	 options = {
              theme = "auto";      # reprend les couleurs Stylix
              icons_enabled = true;
            };
    };
  };
}
